# Legendaly Environment Configuration
# Copy this file to ~/.legendaly/config/.env and customize your settings

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# OpenAI API Key (REQUIRED)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-api-key-here

# =============================================================================
# CORE SETTINGS
# =============================================================================

# Quote tone/style (default: epic)
# Options: epic, cyberpunk, mellow, retro, neon, zen
TONE=epic

# Output language (default: ja)
# Options: ja, en, zh, ko, fr, es, de
LANGUAGE=ja

# OpenAI model to use (default: gpt-4o-mini)
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-4, gpt-3.5-turbo
MODEL=gpt-4o-mini

# Custom category for quotes (optional)
# Example: "fantasy", "philosophy", "technology"
CATEGORY=

# Custom user prompt (optional)
# Example: "Generate quotes about perseverance"
USER_PROMPT=

# =============================================================================
# GENERATION SETTINGS
# =============================================================================

# Number of quotes to generate per batch (default: 25, range: 1-1000)
QUOTE_COUNT=25

# Interval between quote displays in seconds (default: 1, range: 1-300)
FETCH_INTERVAL=1

# Minimum rating filter (default: 0, range: 0-5)
MIN_RATING=0

# =============================================================================
# ANIMATION & DISPLAY
# =============================================================================

# Typewriter speed in milliseconds (default: 40, range: 1-1000)
TYPE_SPEED=40

# Number of fade steps (default: 8, range: 1-50)
FADE_STEPS=8

# Delay between fade steps in milliseconds (default: 100, range: 10-5000)
FADE_DELAY=100

# Display time for each quote in milliseconds (default: 2000, range: 100-60000)
DISPLAY_TIME=2000

# Display style (default: standard)
# Options: standard, compact, detailed
DISPLAY_STYLE=standard

# =============================================================================
# VISUAL EFFECTS
# =============================================================================

# Figlet font for headers (default: slant)
# Common options: slant, big, small, banner, doom, standard
FIGLET_FONT=slant

# Combined tones (comma-separated, overrides TONE)
# Example: "epic,zen", "cyberpunk,neon"
TONES=

# =============================================================================
# FEATURES
# =============================================================================

# Enable interactive mode (default: false)
# Allows rating, saving favorites, and other interactions
INTERACTIVE=false

# Enable verbose logging (default: false)
# Shows detailed timing and processing information
VERBOSE=false

# =============================================================================
# NOTIFICATIONS
# =============================================================================

# Enable notifications (default: false)
NOTIFY=false

# Enable visual notifications (default: false)
VISUAL_NOTIFY=false

# Disable sound notifications (default: true)
DISABLE_SOUND=true

# Enable screen flash notifications (default: false)
FLASH_SCREEN=false

# =============================================================================
# AUDIO (OPTIONAL)
# =============================================================================

# Path to audio file to play with quotes (optional)
# Example: "/path/to/notification.wav"
AUDIO_FILE=

# =============================================================================
# ADVANCED SETTINGS
# =============================================================================

# Custom OpenAI client path (optional)
# Default: ~/.config/common/openaiClients.js
OPENAI_CLIENT_PATH=

# =============================================================================
# EXAMPLES
# =============================================================================

# Zen mode with English output:
# TONE=zen
# LANGUAGE=en
# TYPE_SPEED=30
# DISPLAY_TIME=4000

# Fast cyberpunk mode:
# TONE=cyberpunk
# TYPE_SPEED=20
# FADE_STEPS=5
# FETCH_INTERVAL=0.5

# Interactive philosophy mode:
# CATEGORY=philosophy
# INTERACTIVE=true
# TONE=zen
# MIN_RATING=3